import assert from 'node:assert'
import { it, describe, before, beforeEach } from 'node:test'
import SwigInitializer from '../src/SwigInitializer.js'
import path from 'node:path'

describe('SwigInitializer.temp', () => {
  it('works', () => {
    console.log('hello world from the first placeholder test')
  })
})
